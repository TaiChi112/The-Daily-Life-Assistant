name: 3. The Secretary (Automation)

on:
  # ทำงานเมื่อมีการเปิด Issue ใหม่
  issues:
    types: [opened]
  # ทำงานเมื่อมีการเปิด Pull Request ใหม่
  pull_request:
    types: [opened]

# สำคัญมาก! ต้องขออนุญาตเขียนข้อมูลลง Repo (Permissions)
permissions:
  issues: write
  pull-requests: write

jobs:
  welcome-and-label:
    runs-on: ubuntu-latest
    steps:
      - name: Add Label & Comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # กุญแจวิเศษที่มีอยู่แล้ว
          ISSUE_URL: ${{ github.event.issue.html_url || github.event.pull_request.html_url }}
        run: |
          # 1. ลองสร้าง Label ก่อน (ถ้ามีอยู่แล้ว มันจะข้ามไป ไม่ error)
          gh label create "triage" --color "FBCA04" --description "Needs triage" || true

          # 2. แปะป้าย Label
          gh issue edit "$ISSUE_URL" --add-label "triage"

          # 3. คอมเมนต์ต้อนรับ
          gh issue comment "$ISSUE_URL" --body "👋 สวัสดีครับ! ขอบคุณที่แจ้งเรื่องเข้ามานะ พ่อบ้านรับเรื่องไว้แล้วครับ เดี๋ยวเจ้านายจะรีบมาตรวจสอบครับ @${{ github.actor }}"
